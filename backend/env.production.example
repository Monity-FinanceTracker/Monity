# ===========================================
# MONITY BACKEND - PRODUCTION ENVIRONMENT
# ===========================================
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file with real values to git

# Environment
NODE_ENV=production
PORT=3001

# CORS - Add your production domain
CLIENT_URL=https://app.yourdomain.com

# ===========================================
# SUPABASE CONFIGURATION (REQUIRED)
# ===========================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_KEY=your_supabase_service_role_key_here

# ===========================================
# SECURITY (REQUIRED)
# ===========================================
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your_32_character_encryption_key_here

# ===========================================
# STRIPE CONFIGURATION (OPTIONAL)
# ===========================================
# Only required if using premium features/payments
STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key
STRIPE_PREMIUM_PRODUCT_ID=prod_your_product_id
STRIPE_PREMIUM_PRICE_ID=price_your_price_id
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# ===========================================
# REDIS (OPTIONAL)
# ===========================================
# Optional - improves performance but not required
# Leave empty to use in-memory cache
REDIS_URL=redis://localhost:6379

# ===========================================
# AI FEATURES (OPTIONAL)
# ===========================================
# Only required if using AI categorization features
GEMINI_API_KEY=your_gemini_api_key_here

# ===========================================
# NOTES
# ===========================================
# 1. Generate secure keys using the commands in the deployment guide
# 2. Update CLIENT_URL to match your actual domain
# 3. Never commit this file with real values
# 4. Keep backups of your keys in a secure location

